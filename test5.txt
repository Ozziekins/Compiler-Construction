func do_math(x, y, z) is
    var count := 0;
    while x >= y loop
        if y > z then
            y := y + 1;
        else
            z := z - 1;
        end
    count := count + 1;
    end
    return count;
 end
{
var c;
c := do_math(10, 5, 7);
print c;
}
